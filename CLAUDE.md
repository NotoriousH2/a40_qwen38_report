# 다음 세션을 위한 안내

이 저장소는 **완결된 실험 기록**입니다. 새로 탐색하기 전에 아래를 먼저 읽으세요.
이미 재보고 결론이 난 것을 다시 재는 낭비를 막기 위한 문서입니다.

## 지금 바로 서버를 띄우려면

```bash
./scripts/bootstrap.sh     # 상태 감지 후 필요한 것만 수행 (있으면 40초)
./scripts/verify.sh        # 7항목 검증
```

경로·포트는 전부 `config.env`. 서버는 `http://127.0.0.1:8080/v1` (OpenAI 호환).

## 이미 결론이 난 것 (다시 재지 마세요)

| 질문 | 답 | 근거 |
|---|---|---|
| `-ub` 를 올리면 prefill 이 빨라지나 | **아니오.** 256/1024/2048 모두 무변화 | docs/04 |
| `--spec-draft-n-max` 를 8까지 올리면 | **더 느려짐.** 4가 최적 | docs/04 |
| KV 를 q8_0 → f16 으로 | **빨라짐.** acceptance 0.56→0.66 | docs/04 |
| 장문에서 DFlash2 를 꺼야 하나 | **아니오.** 64K 에서도 1.7~2배 이득 | docs/05, docs/07 |
| Q4_K_XL 로 올릴 가치가 있나 | **없음.** ppl -0.94% 에 속도 -36% | docs/06 |
| 긴 컨텍스트의 병목은 | **prefill.** decode 는 110K 에서도 멀쩡 | docs/05 |
| prefill 을 더 빠르게 할 수 있나 | **거의 불가.** A40 하드웨어 한계 근처 | docs/05 |

## 아직 안 해본 것

- `-np` 를 3 이상으로 올렸을 때의 동시 처리 특성
- `--spec-draft-p-min` 튜닝 (스크립트는 있으나 미측정)
- `--slot-save-path` 로 저장된 슬롯의 재시작 후 복원 실측
- Q5/Q6 등 중간 양자화 등급 (Q3 vs Q4 만 비교함)
- 코딩 능력에 대한 실제 벤치마크 (perplexity 는 약한 대리 지표)

## 측정할 때 반드시 지킬 것

docs/07 에 자세히 있지만 두 줄로 요약하면:

1. **decode 속도는 256토큰 이상 생성해서 재세요.** 짧게 재면 투기 디코딩이 부당하게 불리하게
   나옵니다. `/completion` + `ignore_eos:true` + `n_predict` 고정을 쓰세요.
2. **반복적인 filler 로 컨텍스트를 만들지 마세요.** acceptance 가 1.0 으로 부풀어 최선값만
   보게 됩니다. `bench/realcode.py` 처럼 실제 소스 코드를 쓰세요.

## 건드리면 안 되는 것

`scripts/run-server-spec.sh` 는 최초 요구사항의 옵션을 한 글자도 바꾸지 않은 재현용입니다.
성능 개선 목적으로 이 파일을 수정하지 마세요. 개선은 `run-server-dev.sh` 쪽에 하세요.
